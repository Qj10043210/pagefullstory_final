<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.defaulting">
	<update id="defaulted1">
		DROP TABLE IF EXISTS alt.logged	  
	</update>
	<update id="defaulted2">				  
 		DROP TABLE IF EXISTS alt.rooms		
	</update>
	<update id="defaulted3">		
		DROP TABLE IF EXISTS alt.board
	</update>
	<update id="defaulted4">		
		DROP TABLE IF EXISTS alt.game;
	</update>
	<update id="defaulted1-1" parameterType="java.lang.String">
		${value}
	</update>	
</mapper>